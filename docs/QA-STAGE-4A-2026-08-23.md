# Stage 4A QA — 2026-08-23

Commit probado: `f64b8f40a3548ae0d74b5f4dc6f6348fca2d8e68`

Resultado global: **PASS**, con `WHATSAPP LIVE` bloqueado únicamente por ausencia de una sesión WhatsApp QA ya autorizada. No se realizó pairing ni se utilizó una cuenta personal crítica.

## Evidencia

- `npm ci`: PASS.
- typecheck/tests: PASS, 164/164.
- `npm audit --omit=dev --audit-level=high`: PASS, 0 vulnerabilidades runtime high+.
- Docker ARM64: PASS.
- Poppler en imagen: `pdfinfo` 22.12.0 y `pdftotext` 22.12.0 en `/usr/bin`.
- DB nueva: migraciones 1→14 PASS.
- upgrade v13→v14 sobre fixture Stage 3: PASS, datos y tablas previas intactos; v14 idempotente.
- extracción PDF real: PASS, 1 página, 639 bytes, token sintético recuperado.
- persistencia: PASS, una sola fila por `message_id`, SHA-256/byte length/page count correctos y sin blob PDF raw.
- FTS: PASS; `source=document` solo en `self_memory_fts`, sin contaminación de `observation_fts`.
- caption non-execution: PASS para `anota`, `agenda` e `ia`.
- cleanup temporal: PASS en éxito y fallo controlado.
- timeout determinístico del subprocess: no ejecutado por seguridad/reproducibilidad; queda pendiente de prueba específica si se considera necesario.
- límites declared/actual size, MIME, magic `%PDF-` y page limit: PASS.
- audit/privacy: PASS; sin filename, texto extraído, SHA completo, stdout/stderr Poppler, paths temporales ni captions.
- scan DB/WAL/SHM: no aparece `%PDF-`; texto extraído sí se persiste por diseño.
- backup/restore: PASS con `documents` + FTS funcional tras restore.
- recursos: ~41 ms para PDF de 1 página, ~24 ms para PDF de 10 páginas en el entorno QA; RSS estable alrededor de 116–117 MB; DB QA 228 KB.

## Bloqueado

### WhatsApp live

No había sesión WhatsApp QA autorizada disponible en el worktree aislado. Por política de seguridad no se hizo pairing nuevo ni se usó una cuenta personal crítica.

## Hallazgos

No se encontraron bugs ni riesgos nuevos de Stage 4A en los checks ejecutados. El host fuera de Docker no tenía Poppler; la evidencia funcional se tomó dentro de la imagen del proyecto, que lo incluye por Dockerfile y es el entorno soportado.

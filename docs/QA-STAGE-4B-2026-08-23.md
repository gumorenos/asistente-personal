# QA Stage 4B — 2026-08-23

## Resultado

**OVERALL: PASS**

SHA probado: `9eebbd4b10185939555b2f273a42d5a87e26edce`
Branch: `feat/stage-4b-local-ocr`
PR: #5

El QA fue ejecutado por un runner externo en worktree aislado/detached y sin modificar el repositorio. WhatsApp live quedó bloqueado únicamente por ausencia de una sesión QA autorizada; no se realizó pairing nuevo ni se usó una cuenta personal.

## Base gates

- `npm ci`: PASS.
- `npm run check`: PASS, 177/177 tests.
- `npm audit --omit=dev --audit-level=high`: PASS, 0 vulnerabilidades.
- Docker ARM64 desde el SHA exacto: PASS.

## Runtime ARM64 y herramientas

Imagen QA: `linux/arm64`, `aarch64`, Node 22.18.0.

- `pdfinfo`: 22.12.0.
- `pdftotext`: 22.12.0.
- `pdftoppm`: 22.12.0.
- Tesseract: 5.3.0 / Leptonica 1.82.0.
- traineddata disponibles: `eng`, `osd`, `spa`.

## OCR image-only

PASS.

Fixture sintético image-only de 1 página / 139155 bytes:

- `pdftotext` devolvió solo form-feed y no recuperó el token de prueba;
- con OCR enabled se ejecutó exactamente el fallback esperado: `pdfinfo=1`, `pdftoppm=1`, `tesseract=1`;
- el token se recuperó literalmente;
- se persistió exactamente una fila en `documents`, 1 página, 252 caracteres, `truncated=0`;
- `self_memory_fts` recibió exactamente una entrada `source=document`;
- `busca documentos <token>` encontró el documento.

Con OCR disabled el mismo tipo de documento no produjo persistencia.

## Text-layer no fallback

PASS.

Un PDF normal con capa de texto fue resuelto por Poppler sin ejecutar `pdftoppm` ni Tesseract. El documento se persistió por la ruta normal `text-layer`.

## Límites

PASS.

- `DOCUMENTS_OCR_MAX_PAGES`: PDF de 6 páginas con límite 3 fue rechazado antes de rasterizar; 0 llamadas `pdftoppm`, 0 llamadas Tesseract, 0 documentos.
- DPI configurado a 150 produjo `pdftoppm -r 150`.
- límite de texto 120 produjo exactamente 120 caracteres persistidos, `truncated=1` y detuvo el trabajo tras la primera página.
- configuración inválida rechazada: OCR sin `DOCUMENTS_ENABLED`, `fra`, DPI 99/301 y maxPages 51.
- timeout/deadline determinístico produjo fallo controlado antes de OCR y sin estado parcial.

## Cleanup

PASS.

No quedaron `/tmp/assistant-ocr-*` ni `/tmp/assistant-pdf-*` tras:

- OCR exitoso;
- rechazo por page limit;
- timeout/deadline.

## Audit y privacidad

PASS.

Audit conserva únicamente metadata estructural (`extractor`, `method`, bytes, páginas, caracteres, truncado). No se observó:

- contenido OCR;
- token QA;
- filename del fixture;
- SHA-256 completo;
- stdout/stderr;
- paths temporales.

Tras checkpoint/cierre no se encontraron firmas `%PDF-` ni PNG persistidas en SQLite; WAL/SHM no quedaron presentes.

## Boundary de ejecución

PASS.

El texto OCR incluyó comandos sintéticos equivalentes a `anota`, `agenda` e `ia`. Resultado:

- notas creadas: 0;
- `action_requests`: 0;
- llamadas AI: 0.

Un caption documental con comandos también permaneció terminal dentro de `DocumentCapability`.

## Backup / restore

PASS.

Una copia/restauración del DB conservó el documento OCR y su entrada FTS; la búsqueda del token siguió funcionando.

## Recursos observados

- 1 página image-only: ~1.24 s, PDF ~139 KB, 252 chars, pico cgroup ~126 MB.
- 6 páginas image-only: ~5.96 s, PDF ~621 KB, 1030 chars, 6 llamadas Tesseract, pico cgroup ~128 MB.

Estos valores son observaciones de QA, no SLAs.

## WhatsApp live

**BLOCKED**, no FAIL.

No existía sesión WhatsApp QA autorizada en el worktree aislado. No se realizó pairing nuevo ni se utilizó una sesión personal.

## Conclusión

Stage 4B queda cerrado a nivel de desarrollo y QA runtime local/ARM64. El único pendiente relacionado es la validación WhatsApp live cuando exista una sesión QA autorizada.

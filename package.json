{
  "name": "asistente-personal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.ts",
    "start": "node src/index.ts",
    "doctor": "node src/ops/doctor.ts",
    "backup": "node src/ops/backup-cli.ts create",
    "backup:verify": "node src/ops/backup-cli.ts verify",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "baileys": "7.0.0-rc13"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.8.3"
  }
}
